console.log('This is the module made by the self')

function average(arr) {
    
    let sum = 0;
    // arr.foreach(element => {

    //     sum += element
    // });
    arr.foreach(element => {
        
    });

    return sum / arr.length;

}

module.exports